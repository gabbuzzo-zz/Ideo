<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:vm="clr-namespace:Ideo.ModelViews" xmlns:views="clr-namespace:Ideo.Views"
             x:Class="Ideo.Views.RegisterPage">
    <ContentPage.BindingContext>
        <vm:RegisterViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Username" Text="{Binding Email}"/>
            <Entry IsPassword="True" Placeholder="Password" Text="{Binding Password}"/>
            <Entry IsPassword="True" Placeholder="Password Confirm" Text="{Binding ConfirmPassword}"/>
            <Button  Command="{Binding RegisterCommand}" Clicked="Button_Clicked" Text="Registrati"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>